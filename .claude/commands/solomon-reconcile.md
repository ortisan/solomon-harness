---
description: Standing reconciliation — converge closed issues in memory and on the canonical board
argument-hint: (optional) --dry-run
allowed-tools: Bash(uv run:*)
---

Read and follow `solomon_harness/catalog/workflows/solomon-reconcile.md`.
Treat `$ARGUMENTS` as the value of `{{arguments}}` in that workflow.
