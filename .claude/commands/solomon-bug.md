---
description: File a structured bug report, label and prioritize it, place it on the board Backlog, and record it in memory.
argument-hint: <bug description, with repro steps / env / severity if known>
allowed-tools: Bash(gh:*), Bash(git:*), Bash(uv run:*), Task, Read, Write, AskUserQuestion, mcp__solomon-memory__log_issue, mcp__solomon-memory__log_handoff, mcp__solomon-memory__save_session, mcp__solomon-memory__link_session_handoff
---

Read and follow `solomon_harness/catalog/workflows/solomon-bug.md`.
Treat `$ARGUMENTS` as the value of `{{arguments}}` in that workflow.
