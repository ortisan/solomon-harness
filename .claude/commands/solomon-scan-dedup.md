---
description: Standing maintenance loop — find one duplicated abstraction and open a single draft PR that unifies it
argument-hint: (optional) a path or module to focus the scan
allowed-tools: Bash(gh:*), Bash(git:*), Bash(uv run:*), Task, Read, Grep, Glob, AskUserQuestion, mcp__solomon-memory__save_decision, mcp__solomon-memory__log_issue
---

Read and follow `solomon_harness/catalog/workflows/solomon-scan-dedup.md`.
Treat `$ARGUMENTS` as the value of `{{arguments}}` in that workflow.
