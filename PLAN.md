# PLAN — #9 product_owner skills to canonical depth

Slice of epic #6 (delivery-spine skill depth upgrade). Closes #9.

## Problem statement

`product_owner` has 15 skills, but its role-core ones are short skeletons (~80–240 words) that name good methods yet stop before depth: they start with `##` (not a `#` H1 the canonical format and `document-skills.py` expect), carry no `## Common pitfalls` / `## Definition of done`, and give no worked examples. The canonical reference (`auth_engineer`) is 8 skills at 2,400–3,800 words with those sections. This slice brings the PO role-core skills to canonical depth and consolidates thin duplicates. Content only — no code behavior changes.

## Proposed change and the boundary it touches

Boundary: `agents/product_owner/skills/` (markdown only) plus the regenerated `agents/product_owner/agents/product_owner.md` Active Skills list. No Python, no public contract, no data model. The host integration (`.claude/agents/product_owner.md`) is regenerated by `compile`.

## Target files

Deepen to canonical depth (add `#` H1 + summary + worked example + `## Common pitfalls` + `## Definition of done`, ≥ ~600 words, keep the existing named methods):
- `prioritization.md` (176w) — add a worked RICE/MoSCoW example with real inputs.
- `user_stories_invest.md` (182w) — add splitting patterns + a worked split example.
- `acceptance_criteria_given_when_then.md` (134w) — add a full multi-scenario worked example.
- `the_prd_contract_template.md` (239w) — expand each section with guidance + an example.
- `requirements_traceability.md` — add the RTM key convention + a worked row.
- `product_discovery_and_jtbd.md` — add a JTBD forces example.
- `roadmapping_and_release_planning.md` — add a now/next/later example + release-slice guidance.
- `scope_boundaries.md` (97w) — deepen; absorb `scope_of_this_role`.
- `encoding_other_specialists_requirements_into_acceptance_criteria.md` (348w) — format to canonical + sections.

Consolidate (remove after folding):
- `scope_of_this_role.md` (90w) → into `scope_boundaries.md`.
- `deliverables_and_definition_of_ready.md` (82w) → into `the_prd_contract_template.md`.
- `project_workflow_integration.md` (81w) → into `roadmapping_and_release_planning.md`.

Leave concise (shared checklist files — do NOT force-deepen): `definition_of_done.md`, `common_pitfalls_to_avoid.md`. Light touch (already adequate): `delivery_forecasting_and_flow_metrics.md`.

## Edge cases as observable outcomes

- Each deepened skill starts with exactly one `# ` H1 so `document-skills.py` extracts the first paragraph as the summary (no `##`-led files).
- Each deepened role-core skill has exactly one `## Common pitfalls` and one `## Definition of done`.
- No skill restates `agents/AGENTS.md` shared rules verbatim (single-concern, self-contained).
- The three consolidated files are deleted with no dangling reference left in the profile or other skills.
- `document-skills.py` regenerates the PO Active Skills list with one clean line per skill (no raw-bullet artifacts).

## TDD-style breakdown (one commit each, body ends `Refs #9`)

1. Deepen the three core craft skills: `prioritization`, `user_stories_invest`, `acceptance_criteria_given_when_then`.
2. Deepen `the_prd_contract_template` (+ fold `deliverables_and_definition_of_ready`), `requirements_traceability`.
3. Deepen `product_discovery_and_jtbd`, `roadmapping_and_release_planning` (+ fold `project_workflow_integration`).
4. Deepen `scope_boundaries` (+ fold `scope_of_this_role`), format `encoding_other_specialists_...`.
5. Regenerate: `uv run python scripts/document-skills.py` + `uv run python -m solomon_harness.cli compile`; verify the depth check passes and the suite is green.

"Red/green" here = the depth check (below) fails before each step and passes after.

## STRIDE notes

N/A — content-only change. No input parsing, auth, data, or external surface is touched.

## ADR decision

Not architecturally significant: no new dependency or datastore, no changed public contract or data model, no cross-cutting code pattern, easily reversible. **No ADR.** (Restated in the PR.)

## Verification criteria (objectively checkable)

- For every PO role-core skill (all `agents/product_owner/skills/*.md` except `definition_of_done.md` and `common_pitfalls_to_avoid.md`): file starts with `# `, contains `## Common pitfalls` and `## Definition of done`, is ≥ 600 words, and names ≥ 1 concrete method/standard.
- `scope_of_this_role.md`, `deliverables_and_definition_of_ready.md`, `project_workflow_integration.md` no longer exist.
- `uv run python scripts/document-skills.py` exits 0 and updates the PO Active Skills list.
- `uv run python -m solomon_harness.cli compile` leaves tracked source unchanged except the intended skill files + regenerated profile/integration.
- `uv run pytest -q` stays green and `uv run ruff check .` is clean (no Python touched).
